# Desafio 09: Remover Duplicatas

## Descrição
Escreva uma função que recebe uma lista de números e retorna uma nova lista sem elementos duplicados, mantendo a ordem original dos elementos. **Não use `set()` ou métodos similares**.

## Exemplo
```python
remover_duplicatas([1, 2, 2, 3, 4, 4, 5])
# Retorna: [1, 2, 3, 4, 5]

remover_duplicatas([3, 3, 3, 3])
# Retorna: [3]

remover_duplicatas([1, 1, 2, 2, 3, 3])
# Retorna: [1, 2, 3]
```

## Assinatura da Função
```python
def remover_duplicatas(lista: list[int]) -> list[int]:
    pass
```

